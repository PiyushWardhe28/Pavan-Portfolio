import React from 'react';
import ReactDOM from 'react-dom';
import App from './App'; // You will need to create `App.js` in the same folder or adjust the path.

ReactDOM.render(<App />, document.getElementById('root'));
